use application;

create table ecommerceApplication(

CEO varchar(30),
operating_System varchar(25),
companyType varchar(50),
founded_Year int,
products_Available varchar(100)
);

desc ecommerceApplication;